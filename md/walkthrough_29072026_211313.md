# 🔔 FCM Integration — Plan & Task List (Option A)

## Kiến trúc mục tiêu

```
App (Expo)
  └─ getExpoPushTokenAsync()  →  ExponentPushToken[xxx]
       │
       ▼
  Backend POST token → DB: expo_push_tokens (manv, platform, token)
       │
       ▼  (khi cần gửi)
  Backend → https://exp.host/--/api/v2/push/send
       │
       ▼
  Expo Push Service  ←── FCM v1 Service Account (cần setup)
       │
       ▼
  FCM → Android Device  /  APNs → iOS Device
```

---

## Trạng thái hiện tại

| # | Hạng mục | Trạng thái |
|---|----------|-----------|
| DB schema `expo_push_tokens` | PK = `manv` (1 thiết bị/người) | ✅ User đã fix ở Postgres |
| SQL Function `expo_push_token_register` | `ON CONFLICT (manv, platform)` | ✅ Đã sửa |
| `app.json` — `googleServicesFile` | Đã thêm `"./google-services.json"` | ✅ Done |
| `.gitignore` | Đã thêm `google-services.json` | ✅ Done |
| Firebase Project + `google-services.json` | Chưa có file | ❌ Cần làm |
| FCM v1 Service Account → Expo Console | Chưa upload | ❌ Cần làm |
| Backend: `badge` trong payload | Không có | ❌ Cần sửa |
| Backend: Multi-token (gửi tất cả thiết bị) | `LIMIT 1` — chỉ gửi 1 | ❌ Cần sửa |

---

## Task List

### 🔴 STEP 1 — Firebase Setup (Thủ công)

- [ ] Truy cập [https://console.firebase.google.com](https://console.firebase.google.com)
- [ ] Tạo Project (hoặc dùng project hiện có)
- [ ] Thêm **Android App** với package name: `com.duyvanda.biportal`
- [ ] Download `google-services.json` → đặt vào **root project**: `d:\django_apps\rest\fontendapp\google-services.json`
- [ ] Vào **Project Settings → Service Accounts → Generate new private key** → tải file `.json` (dùng cho Expo Console)

---

### 🔴 STEP 2 — Expo Console: Upload FCM v1 Credentials (Thủ công)

- [ ] Truy cập [https://expo.dev](https://expo.dev) → Project `bi-portal`
- [ ] Vào **Credentials → Android**
- [ ] Chọn **FCM V1 Service Account Key** → Upload file `.json` vừa tải ở STEP 1
- [ ] Save

> [!IMPORTANT]
> Đây là bước **bắt buộc** nhất. Không có FCM v1 credentials, Expo không thể relay push tới Android sau khi Google tắt Legacy API (6/2024).

---

---

### 🟡 STEP 4 — Backend: Sửa `api_send_push_notification` (local_views.py:5066)

**Cần sửa 2 điểm trong [`local_views.py`](file:///d:/django_apps/rest/frontend1/bi_local_rest/local_views.py#L5066-L5103):**

#### A. Lấy TẤT CẢ tokens thay vì LIMIT 1:
```python
# ❌ Hiện tại (line 5073)
cur.execute("SELECT token FROM expo_push_tokens WHERE manv = %s LIMIT 1;", (manv,))
token_row = cur.fetchone()

# ✅ Cần sửa thành
cur.execute("SELECT token FROM expo_push_tokens WHERE manv = %s;", (manv,))
token_rows = cur.fetchall()
```

#### B. Thêm `badge` + `sound` và loop qua tất cả tokens:
```python
# Lấy unread count để set badge
cur.execute("""
    SELECT COUNT(*) FROM expo_notifications
    WHERE manv = %s AND is_read = FALSE;
""", (manv,))
unread_row = cur.fetchone()
unread_count = int(unread_row[0]) if unread_row else 1

# Gửi tới tất cả thiết bị
import requests
device_results = []
for (expo_token,) in token_rows:
    expo_payload = {
        "to": expo_token,
        "title": title,
        "body": body,
        "sound": "default",       # ← THÊM
        "badge": unread_count,    # ← THÊM
        "data": {
            "type": noti_type,
            "report_stt": report_stt
        }
    }
    resp = requests.post(expo_url, json=expo_payload, headers={
        'Content-Type': 'application/json',
        'Accept': 'application/json',
        'Accept-Encoding': 'gzip, deflate',
    })
    device_results.append({...})

results.append({"status": "success", "manv": manv, "devices_sent": len(token_rows), "device_results": device_results})
```

- [ ] Sửa `LIMIT 1` → lấy tất cả tokens
- [ ] Thêm query unread_count
- [ ] Thêm `badge` + `sound` vào payload
- [ ] Loop gửi tới từng token

---

---

## Lưu ý quan trọng

> [!WARNING]
> `google-services.json` đã được thêm vào `.gitignore` → **KHÔNG commit file này lên Git**.
> File này cần tồn tại local khi build EAS. Nếu dùng CI/CD, phải dùng **EAS Secrets** hoặc truyền qua environment variable.

> [!NOTE]
> `.easignore` **không cần** thêm `google-services.json` — EAS Build cần đọc file này để compile native Android module.
