# 🚀 BÁO CÁO CẢI TIẾN & TÍNH NĂNG XOAY MÀN HÌNH BÁO CÁO NHÚNG (TIKTOK STYLE)

> **Cập nhật gần nhất**: `2026-07-24 21:42:54 (+07:00)`

---

## 🔍 1. TỔNG QUAN XỬ LÝ LỖI & NÂNG CẤP KIẾN TRÚC

### 1.1 Khắc phục lỗi Nút Chụp Hình bị Lặp (Duplicate Capture Button)
* **Nguyên nhân**: `ReportWebView.tsx` đã có sẵn 1 nút chụp hình nổi kéo thả. Việc khai báo thêm 1 nút chụp hình nổi ở các file màn hình bọc ngoài `report/[id].tsx` và `realtime/[id].tsx` làm phát sinh 2 nút đè lên nhau khi WebView load xong.
* **Đã xử lý**:
  - Gỡ bỏ hoàn toàn code nút chụp hình thừa ở [report/[id].tsx](file:///d:/django_apps/rest/fontendapp/src/app/report/[id].tsx) và [realtime/[id].tsx](file:///d:/django_apps/rest/fontendapp/src/app/realtime/[id].tsx).
  - Tối ưu nút chụp trong [ReportWebView.tsx](file:///d:/django_apps/rest/fontendapp/src/components/ReportWebView.tsx): Bổ sung hiệu ứng đồng hồ cát (`hourglass-outline`) khi đang chụp và chặn bấm liên tục để tránh đúp bồ.

---

### 1.2 Nâng cấp Kiến trúc Xoay Màn Hình (TikTok-Style Rotation) & Fix Lỗi Phản Biện Kỹ Thuật
* **Định hướng UX**:
  - Giữ toàn bộ ứng dụng ở chế độ chiều Dọc cố định (`"orientation": "portrait"`) trong [app.json](file:///d:/django_apps/rest/fontendapp/app.json#L6) để tránh ứng dụng bị xoay nhầm khi người dùng cầm nghiêng điện thoại.
  - Tích hợp **Nút Xoay Màn Hình phong cách TikTok/YouTube** vào cụm Nút Nổi Kéo Thả (Draggable Panel) dành riêng cho các báo cáo nhúng Looker Studio ([ReportWebView.tsx](file:///d:/django_apps/rest/fontendapp/src/components/ReportWebView.tsx)).
  - **Tự động Ẩn Header (Fullscreen Immersive)**: Khi ở chế độ xoay ngang, Header top được tự động ẩn đi để nhường 100% diện tích chiều cao màn hình cho báo cáo Looker Studio.
  - **Hỗ trợ cầm máy linh hoạt (`LANDSCAPE`)**: Không ép cứng `LANDSCAPE_RIGHT`, hỗ trợ tự động cảm biến nghiêng trái/nghiêng phải theo thói quen của người dùng.
  - **Khóa toạ độ mép phải (`btn_x`)**: Tự động re-anchor cụm nút nổi sát mép phải màn hình khi xoay ngang, không bị trôi lơ lửng ở giữa màn hình.

---

### 1.3 Dọn dẹp Mã Nguồn (Code Cleanup) & Khắc phục Lỗi Biên Dịch
* **Reset Zoom khi Xoay Màn hình**: Tự động reset `native_scale`, `manual_pan_x`, và `manual_pan_y` về tỉ lệ 100% khi chuyển đổi chiều màn hình trong [ReportWebView.tsx](file:///d:/django_apps/rest/fontendapp/src/components/ReportWebView.tsx) để tránh lệch khung nhìn (viewport offset).
* **Dọn dẹp Dead Code**: Loại bỏ hàm thừa `handle_logout`, state `show_menu`, và các `import` dư thừa trong [CustomHeader.tsx](file:///d:/django_apps/rest/fontendapp/src/components/CustomHeader.tsx).
* **Sửa lỗi TypeScript `expo-device`**: Sửa truy cập thuộc tính `Device.deviceName` trong [NotificationContext.tsx](file:///d:/django_apps/rest/fontendapp/src/context/NotificationContext.tsx), đưa kết quả biên dịch TypeScript đạt **0 lỗi (`Zero Errors`)**.

---

## 🛠️ 2. CHI TIẾT CÁC THAY ĐỔI TRONG MÃ NGUỒN

### 2.1 Cấu hình [app.json](file:///d:/django_apps/rest/fontendapp/app.json)
- Trả `"orientation": "portrait"` để khóa cứng chiều dọc toàn app.

### 2.2 Cài đặt Gói Thư viện
- Cài đặt `expo-screen-orientation` bằng lệnh `npx expo install expo-screen-orientation`.

### 2.3 Nâng cấp Component [ReportWebView.tsx](file:///d:/django_apps/rest/fontendapp/src/components/ReportWebView.tsx)
1. **Quản lý State & Lifecycle Xoay**:
   - Thêm state `is_landscape` và callback `on_orientation_change` báo về cho màn hình cha.
   - Thêm hàm `handle_toggle_orientation()`: Reset tỉ lệ zoom về 100% và chuyển đổi linh hoạt giữa `LANDSCAPE` và `PORTRAIT_UP`.
   - Bổ sung `useEffect` cleanup an toàn với `.catch(() => {})`: Luôn khôi phục chiều dọc `PORTRAIT_UP` khi unmount hoặc khi người dùng bấm Back.
2. **Neo Vị trí Cụm Nút Nổi sát Mép Phải**:
   - Bổ sung `useEffect` cập nhật `btn_x.value = screen_w - BTN_PANEL_W - 12` khi `screen_w` và `screen_h` thay đổi (khi xoay ngang/dọc) để nút luôn dính sát mép phải chuẩn xác.
3. **Thiết kế Cụm Nút Nổi 2-trong-1**:
   - **Nút 1 (Phía trên)**: 📸 **Chụp hình & Chia sẻ** (`Ionicons: camera`).
   - **Nút 2 (Phía dưới)**: 🔄 **Xoay màn hình TikTok Style** (`MaterialIcons: screen-rotation`). Khi ở chế độ ngang, nút sáng màu chủ đạo `#00A79D`.

### 2.4 Tối ưu [CustomHeader.tsx](file:///d:/django_apps/rest/fontendapp/src/components/CustomHeader.tsx)
- Dọn dẹp triệt để 15+ thuộc tính dư thừa trong `useFeedback()` context destructure, chỉ giữ lại `user_info` để tối ưu hiệu năng và triệt tiêu các lượt re-render không cần thiết.
- Loại bỏ toàn bộ dead code, unused state (`show_menu`), unused context (`useNotification`), và unused imports (`@/utils/string`).
- Bổ sung `paddingLeft: insets.left` và `paddingRight: insets.right` tránh đè Tai thỏ / Dynamic Island.

### 2.5 Cập nhật Màn hình Bọc [report/[id].tsx](file:///d:/django_apps/rest/fontendapp/src/app/report/[id].tsx) & [realtime/[id].tsx](file:///d:/django_apps/rest/fontendapp/src/app/realtime/[id].tsx)
- Nhận event `on_orientation_change` từ `ReportWebView`.
- Ẩn hoàn toàn `CustomHeader` khi ở chế độ ngang (`is_landscape === true`), mang lại trải nghiệm xem báo cáo Fullscreen 100% cực kỳ ấn tượng.

---

## 📱 3. PHẠM VI ÁP DỤNG

Tính năng xoay màn hình bằng nút nổi TikTok-Style được áp dụng tự động cho cả 2 màn hình báo cáo nhúng:
1. 📄 **Báo cáo Tĩnh Nhúng Looker**: [src/app/report/[id].tsx](file:///d:/django_apps/rest/fontendapp/src/app/report/[id].tsx)
2. ⚡ **Báo cáo Realtime Nhúng Looker**: [src/app/realtime/[id].tsx](file:///d:/django_apps/rest/fontendapp/src/app/realtime/[id].tsx) *(Người dùng nhập tham số ngày tháng dạng Dọc chuẩn mực, sau khi load xong báo cáo bấm nút nổi để xoay ngang xem dữ liệu Fullscreen mượt mà)*.

---

## 🧪 4. KẾT QUẢ KIỂM THỬ & TỐI ƯU (VERIFICATION)

- [x] Lỗi trùng lặp nút chụp hình đã được dọn dẹp triệt để.
- [x] Ứng dụng không bị xoay ngoài ý muốn khi cầm nghiêng máy ở các màn hình chính.
- [x] Bấm nút 🔄 xoay ngang / xoay dọc mượt mà, hỗ trợ linh hoạt cả 2 hướng xoay ngang (`LANDSCAPE`).
- [x] Tự động reset zoom level về 100% khi xoay màn hình để giữ khung nhìn luôn chuẩn aspect fit.
- [x] Ẩn Header hoàn toàn ở chế độ xoay ngang, mở rộng diện tích tối đa cho báo cáo.
- [x] Cụm nút nổi tự động dính sát mép phải màn hình sau khi xoay.
- [x] Thoát màn hình báo cáo tự động trả ứng dụng về chiều Dọc an toàn không bị crash/unhandled rejection.
- [x] Loại bỏ dead-code và re-render thừa trong CustomHeader.
- [x] Biên dịch TypeScript thành công 100% (`npx tsc --noEmit` đạt 0 lỗi).
